"""
Настройки разметки по топ-k (без ручной разметки и логов).

Релевантными считаем топ-k кандидатов по результатам выбранного метода ранжирования.
Обоснование: для оценки и экспериментов принимаем, что топ-k — кандидаты,
которых мы бы рассматривали как релевантных (порог отсечки).
"""
# Сколько кандидатов в голове списка считать релевантными (relevance=1)
LABELS_TOP_K = 5

# Альтернативные варианты для экспериментов (например, топ-20)
LABELS_TOP_K_OPTIONS = [5, 10, 20]

# Метод, по результатам которого строим разметку (папка в data/results/)
# Используется при генерации labels через build_labels_from_ranking
LABELS_SOURCE_METHOD = "bm25"
